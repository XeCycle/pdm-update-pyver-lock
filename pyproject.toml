[project]
name = "tester"
version = "0.1.0"
description = "Default template for PDM package"
authors = []
dependencies = ["aiohttp~=3.10"]
requires-python = "==3.8.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
